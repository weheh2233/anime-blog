---
title: Python基础整理
description: >-
  这份文档是基于与ai对话，部分网课，python_api文档而让ai整理的，目标不是面面俱到，而是让你以最快速度进入 Python + FastAPI +
  AI 开发。
author: Kira
publishDate: 2026-07-29
zone: 编程
tags:
  - Python
  - Fastapi
draft: false
pinned: false
---
# Python 基础整理

这份文档是基于我们前面的对话整理的，目标不是面面俱到，而是让你以最快速度进入 Python + FastAPI + AI 开发。

适合你当前阶段的使用方式：

- 当复习手册看
- 当查漏补缺清单看
- 当进入 FastAPI / LangChain 前的过渡材料看

---

## 1. 现在学到哪了

你已经掌握或接触过这些核心块：

- 异常处理：`try / except`、`raise`、`Exception`
- 迭代器：`iter()`、`next()`、`StopIteration`
- 生成器：`yield`
- `range()`
- 字符串格式化
- `bytes` 和 `str`
- `dataclass` 和 `pydantic`
- `venv` / `pip` / `uv`
- 文件读写、JSON
- `async` / `await` / event loop / `gather`

结论：

**Python 基础还没“全学完”，但已经完全够你进入 FastAPI 和 AI 开发。**

---

## 2. 异常处理

### 2.1 Python 里有没有类似 Java `Exception` 的大类

有。

- 更大的根类：`BaseException`
- 日常业务代码主要接：`Exception`

```python
try:
    x = int("abc")
except Exception as e:
    print("出错了", e)
```

一般不用随便捕获 `BaseException`，因为像 `SystemExit`、`KeyboardInterrupt` 也在那下面。

### 2.2 `raise` 是什么

`raise` 就是“主动抛异常”。

```python
def divide(a, b):
    if b == 0:
        raise ValueError("除数不能为0")
    return a / b
```

类比 Java：

- Python `raise`
- Java `throw`

---

## 3. 迭代器与 `for`

### 3.1 `iter()` 和 `next()` 是什么

Python 的 `for` 循环底层依赖迭代器协议。

- `iter(obj)`：拿到迭代器
- `next(it)`：向后取一个值

如果没有下一个值了，就抛：

```python
StopIteration
```

### 3.2 `for` 会自动调什么

`for x in obj:` 本质类似：

```python
it = iter(obj)

while True:
    try:
        x = next(it)
        print(x)
    except StopIteration:
        break
```

所以你之前问的“`for` 会自动调 `next` 吗”，答案是：**会。**

### 3.3 `__iter__()` 和 `__next__()` 谁先

先 `iter()`，后 `next()`。

流程：

1. `for` 先调用对象的 `__iter__()`
1. 拿到迭代器对象
1. 再不断调用这个迭代器的 `__next__()`

### 3.4 你之前那段反向迭代器怎么理解

正确写法应是：

```python
class Reverse:
    def __init__(self, data):
        self.data = data
        self.index = len(data)

    def __iter__(self):
        return self

    def __next__(self):
        if self.index == 0:
            raise StopIteration
        self.index -= 1
        return self.data[self.index]
```

解释：

- `self.index = len(data)`：一开始指向“最后一个元素的后一位”
- 每次 `next()` 时先减 1
- 再返回当前位置元素

如果 `data = "abcd"`：

- 第一次返回 `d`
- 第二次返回 `c`
- 第三次返回 `b`
- 第四次返回 `a`

---

## 4. 生成器和 `yield`

### 4.1 生成器是什么

生成器是“更省事地创建迭代器”的方法。

你不用自己写 `__iter__()` 和 `__next__()`，只要写 `yield`。

```python
def reverse(data):
    for index in range(len(data) - 1, -1, -1):
        yield data[index]
```

### 4.2 `yield` 是什么感觉

它不是普通 `return`。

- `return`：函数直接结束
- `yield`：先返回一个值，但函数状态保留，下次还能从这里继续

所以你可以把生成器理解成：

**“一个可以暂停和恢复的函数。”**

### 4.3 生成器除了给 `for` 迭代还有什么用

- 处理大文件，节省内存
- 一批一批地产生数据
- 管道式处理数据
- 在异步思维里帮助你理解“暂停再恢复”

示例：

```python
def read_big_file(path):
    with open(path, "r", encoding="utf-8") as f:
        for line in f:
            yield line.strip()
```

这样不会一次把整个文件全读进内存。

---

## 5. `range()`

## 5.1 基本语法

```python
range(start, stop, step)
```

- `start`：开始
- `stop`：结束边界，但不包含
- `step`：步长

### 5.2 倒序例子

```python
range(3, -1, -1)
```

如果索引是 `0 1 2 3`，那它会得到：

```python
3, 2, 1, 0
```

注意：

- 最后那个 `-1` 不是“倒数第一个元素”
- 它只是一个**停止边界**
- 因为 `range` 是“包含起点，不包含终点”

所以：

```python
range(3, -1, -1)
```

意思是：

- 从 3 开始
- 每次减 1
- 一直到大于 `-1` 为止

---

## 6. 字符串格式化

你看过这一段：

```python
yes_votes = 42_572_654
total_votes = 85_705_149
percentage = yes_votes / total_votes
'{:-9} YES votes  {:2.2%}'.format(yes_votes, percentage)
```

### 6.1 数字里的下划线

```python
42_572_654
```

只是为了增强可读性，值还是普通整数。

和：

```python
42572654
```

完全一样。

Java 里也能这样写数字下划线。

### 6.2 `.format()` 基本理解

```python
"{} {}".format(a, b)
```

就是把值按顺序塞进花括号。

### 6.3 百分比格式

```python
"{:.2%}".format(0.496)
```

输出：

```python
49.60%
```

意思是：

- 转成百分数
- 保留两位小数

### 6.4 对齐

右对齐：

```python
"{:>10}".format("hi")
```

左对齐：

```python
"{:<10}".format("hi")
```

居中：

```python
"{:^10}".format("hi")
```

更推荐你以后多用 f-string：

```python
name = "Tom"
score = 95
print(f"{name} score={score}")
```

---

## 7. `str` 和 `bytes`

### 7.1 字符串前面带 `b` 是什么

```python
b"hello"
```

这不是普通字符串，而是 `bytes`。

- `str`：文本
- `bytes`：字节

### 7.2 常见转换

```python
s = "你好"
b = s.encode("utf-8")
print(b)

s2 = b.decode("utf-8")
print(s2)
```

什么时候常见：

- 网络传输
- 文件读写
- 图片/音频/二进制数据

---

## 8. `dataclass` 和 `pydantic`

### 8.1 `dataclass`

适合普通数据对象。

```python
from dataclasses import dataclass

@dataclass
class Student:
    student_id: str
    name: str
    scores: dict[str, int]
```

它会帮你自动生成初始化方法等样板代码。

### 8.2 `pydantic`

适合：

- 接口请求体
- 数据校验
- 配置管理
- AI 输入输出结构化

### 8.3 `BaseModel`

`BaseModel` 是 Pydantic 的核心基类。

```python
from pydantic import BaseModel

class User(BaseModel):
    name: str
    age: int
```

它能做：

- 类型校验
- 自动转换
- 序列化
- 错误提示

### 8.4 嵌套模型

```python
from pydantic import BaseModel

class Address(BaseModel):
    city: str
    zip_code: str

class User(BaseModel):
    name: str
    address: Address
```

这就是嵌套模型。

### 8.5 `Field`

`Field()` 用来补充字段约束和元信息。

```python
from pydantic import BaseModel, Field

class User(BaseModel):
    name: str = Field(min_length=2, max_length=20)
    age: int = Field(ge=0, le=150)
```

常见用途：

- 长度限制
- 数值范围
- 默认值
- 描述信息

---

## 9. 虚拟环境与依赖管理

### 9.1 `venv`

可以理解成：

**一个项目自己的 Python 运行环境。**

类比 Java，不完全等价，但可以先类比成：

- JDK + 项目局部依赖隔离环境

它的作用是避免不同项目的包互相污染。

### 9.2 `pip`

`pip` 是 Python 的包安装工具。

类比：

- Maven/Gradle 里的依赖下载能力

但 `pip` 本身不完全等于 Maven，因为 Maven 还带项目构建生命周期和依赖声明体系。

### 9.3 `uv`

`uv` 可以先理解成：

**更现代、更快的一体化工具。**

它常覆盖：

- Python 版本管理
- 虚拟环境
- 依赖安装
- 锁定依赖

你可以先把它理解成：

**比 `venv + pip` 更顺手的一套现代组合。**

---

## 10. 文件读写与 JSON

### 10.1 文件不存在时能不能自动创建

能。

如果你用写模式打开：

```python
with open("a.txt", "w", encoding="utf-8") as f:
    f.write("hello")
```

如果文件不存在，会自动创建。

### 10.2 往不存在的文件写 JSON

```python
import json

data = {"name": "Tom", "age": 18}

with open("user.json", "w", encoding="utf-8") as f:
    json.dump(data, f, ensure_ascii=False, indent=4)
```

### 10.3 `save_students(student)` 为什么不对

你项目里有这样的问题：

```python
save_students(student)
```

但函数定义是：

```python
def save_students():
```

这说明：

- 函数不接参数
- 它是直接把全局 `students` 整体保存出去

所以正确调用应该是：

```python
save_students()
```

这类问题本质上就是：

**调用签名和函数定义不一致。**

---

## 11. 面向对象补充

### 11.1 `self` 能有多少个属性

理论上不限。

比如：

```python
class User:
    def __init__(self):
        self.name = "Tom"
        self.age = 18
        self.city = "Shanghai"
```

`self` 本质就是“当前对象本身”，你给它挂多少属性，取决于你的设计。

但工程上不要乱挂，最好字段稳定、明确。

### 11.2 `self` 类比什么

类比 Java 的：

```java
this
```

---

## 12. 四大数据结构

这一块你虽然没逐个系统问，但非常值得整理进去，因为 Python 日常开发离不开。

### 12.1 `list`

有序、可变、可重复。

```python
nums = [1, 2, 3]
nums.append(4)
nums[0] = 10
```

常见用途：

- 存一组有顺序的数据
- 遍历
- 收集结果

### 12.2 `tuple`

有序、不可变、可重复。

```python
point = (10, 20)
```

常见用途：

- 表示不想改动的一组值
- 函数返回多个值

```python
def get_user():
    return "Tom", 18
```

### 12.3 `dict`

键值对映射，最常用。

```python
user = {"name": "Tom", "age": 18}
print(user["name"])
user["city"] = "Shanghai"
```

类比 Java：

- `Map<K, V>`

你做接口开发、AI 开发时会大量用到。

### 12.4 `set`

无序、不重复。

```python
s = {1, 2, 3}
s.add(4)
```

常见用途：

- 去重
- 成员判断

```python
visited = set()
```

### 12.5 怎么选

- 要顺序、能改：`list`
- 要顺序、不想改：`tuple`
- 要键值映射：`dict`
- 要去重：`set`

### 12.6 常见遍历

```python
nums = [1, 2, 3]
for n in nums:
    print(n)

user = {"name": "Tom", "age": 18}
for k, v in user.items():
    print(k, v)
```

---

## 13. 模块、包、导入

这块你前面没有系统追问，但接下来学 FastAPI 一定会碰。

### 13.1 模块

一个 `.py` 文件就是一个模块。

### 13.2 包

一个目录里放多个模块，可以组成包。

### 13.3 导入

```python
import json
from pathlib import Path
from mymodule import add
```

FastAPI 项目里后面很常见：

- `routers/`
- `schemas/`
- `services/`
- `models/`

---

## 14. `with`

### 14.1 为什么总写 `with open(...)`

因为 `with` 是上下文管理器语法，能帮你自动收尾。

```python
with open("a.txt", "r", encoding="utf-8") as f:
    text = f.read()
```

好处：

- 用完自动关闭文件
- 异常时也能正确清理

---

## 15. `async` / `await` / event loop

这是你问得最多、也最关键的一块，我集中整理一下。

### 15.1 `async def` 是什么

```python
async def foo():
    return 1
```

这定义的是协程函数。

调用它：

```python
foo()
```

不会立刻得到结果，而是得到一个协程对象。

### 15.2 `await` 是什么

`await` 的作用是：

**等待一个可等待对象完成，并在等待期间把控制权交回 event loop。**

```python
async def main():
    x = await foo()
```

### 15.3 `await` 只能在哪用

只能在 `async def` 里面用。

### 15.4 同步代码怎么进入异步世界

靠：

```python
asyncio.run(main())
```

它会：

1. 创建 event loop
1. 执行协程
1. 等待执行结束
1. 返回结果
1. 关闭 loop

### 15.5 `event loop` 是什么

可以把它理解成：

**异步任务调度器。**

负责：

- 管理协程任务
- 遇到等待时切换任务
- 哪个任务 ready 了就恢复它

### 15.6 `await asyncio.sleep(1)` 和 `time.sleep(1)` 的区别

```python
await asyncio.sleep(1)
```

是异步等待，会把控制权交回 event loop。

```python
time.sleep(1)
```

是同步阻塞，会卡住整个线程。

所以在 `async def` 里写 `time.sleep()`，通常是坏味道。

### 15.7 `gather()`

```python
await asyncio.gather(c(), f())
```

表示把多个协程一起交给 event loop 调度，并等待它们都完成。

### 15.8 你前面问到的关键结论

#### 结论 1

`async def` 不是魔法。

如果里面写的是阻塞代码，比如：

```python
time.sleep(2)
```

那它仍然会卡住 event loop。

#### 结论 2

多个 ready 的协程，**不要依赖谁先执行**。

比如 `c` 和 `f` 都 ready 时：

- 实现层可能像队列
- 但业务上不要假设顺序

#### 结论 3

并发协程要尽量解耦。

不要写成：

- 必须 `c` 先跑
- `f` 才能正确

要写成：

- 谁先谁后都不影响结果

#### 结论 4

`asyncio` 是协作式调度，不是抢占式调度。

也就是说：

- 协程不会在普通同步代码中途被强行打断
- 一般要到 `await` 这个切换点才把控制权交回 loop

---

## 16. AI 开发前最该补牢的几个 Python 点

这些你没全问，但非常值得尽快补上：

### 16.1 函数参数

```python
def add(a, b=1):
    return a + b
```

还包括：

- 位置参数
- 关键字参数
- 默认参数
- `*args`
- `**kwargs`

### 16.2 推导式

```python
nums = [1, 2, 3]
squares = [x * x for x in nums]
```

字典推导式、集合推导式也很常见。

### 16.3 `pathlib`

后面做工程开发，强烈建议多用 `Path`，少手写字符串路径。

```python
from pathlib import Path

path = Path("data.json")
if path.exists():
    print(path.read_text(encoding="utf-8"))
```

### 16.4 `json`

接口开发、AI 开发会反复用：

- `json.loads()`
- `json.dumps()`
- `json.load()`
- `json.dump()`

---

## 17. 接下来最优学习路线

结合你当前背景，推荐这样走：

### 第一阶段：Python 够用收尾

目标：

- 把这份文档里的内容扫熟
- 遇到不懂的点再补

### 第二阶段：快速过 FastAPI

只学这些：

- 路由
- 请求参数
- 请求体
- 响应模型
- `BaseModel`
- `async def`
- 文件上传

### 第三阶段：进入 AI 开发

建议路线：

- OpenAI SDK
- FastAPI 包一层接口
- LangChain 基础
- LangGraph
- RAG
- Tool Calling

### 第四阶段：做小项目

建议至少做 2 到 3 个：

- 聊天接口
- 文档问答
- 简单 Agent

---

## 18. 你当前阶段的真实结论

如果问：

**“Python 基础学完了吗？”**

答案是：

- 没有全学完
- 但已经够进入下一阶段

如果问：

**“要不要继续死磕纯基础？”**

答案是：

- 不要
- 边做 FastAPI、边做 AI、边补 Python

如果问：

**“现在最该去学什么？”**

答案是：

- FastAPI
- Pydantic
- OpenAI SDK / LangChain

---

## 19. 还可以继续补充的专题

这些我们可以下一轮继续整理成文档追加章节：

- 函数参数全家桶：`*args`、`**kwargs`
- 面向对象：继承、`super()`
- 推导式专题
- `pathlib` 专题
- FastAPI 入门讲义
- Pydantic 进阶讲义
- Python 常见陷阱清单

如果继续整理，我建议下一份直接做：

**《FastAPI 快速上手讲义》**
