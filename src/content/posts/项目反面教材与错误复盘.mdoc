---
title: 项目反面教材与错误复盘
description: |-
  比赛期间形成的 LeeGo 可运行功能演示版
  目的：把项目中不成熟的设计、危险的实现、演示性质的代码和工程化缺口完整记录下来，作为后续整改清单、面试复盘材料和 Claude Code 的实施依据。
author: Kira
publishDate: 2026-07-30
zone: 编程
tags:
  - project
draft: false
pinned: false
---
# LeeGo 项目反面教材与错误复盘

> 审计日期：2026-07-30\
审计对象：比赛期间形成的 LeeGo 可运行功能演示版\
文档目的：把项目中不成熟的设计、危险的实现、演示性质的代码和工程化缺口完整记录下来，作为后续整改清单、面试复盘材料和 Claude Code 的实施依据。

## 0. 先说结论

这个项目不是“什么都没有”，也不是“代码完全不能运行”。它的问题是：

> 用比赛演示版的实现方式，承载了一个看起来像真实消费决策平台的产品叙事。

因此最危险的地方不是某一行代码写得丑，而是**数据、权限、AI 输出、前端展示和项目文档之间没有形成可信的边界**。

当前项目最典型的错误可以概括为：

1. 把演示数据当成业务事实。
1. 把 Redis 向量索引当成商品主库的一部分。
1. 把大模型当成搜索排序器、评分器和事实补全器。
1. 把前端 Mock、测试 Token、默认密码留在正式应用链路中。
1. 把用户提交的 `userId`、商品列表和价格当成可信数据。
1. 把“接口能返回结果”误认为“业务链路已经闭环”。
1. 没有自动化测试，却用手工 Apifox 验证结果替代系统性回归。
1. README、数据库结构、前端接口和真实运行路径没有统一契约。

这份文档不是为了否定当时的工作量，而是要把“当时为什么能跑、为什么又不成熟”讲清楚。

## 1. 严重程度

{% table %}
- 等级
- 含义
---
- P0
- 安全、权限、数据真实性或启动可靠性问题；必须优先修复
---
- P1
- 直接影响核心业务正确性、RAG 质量或性能的问题
---
- P2
- 测试、可维护性、可观测性和接口契约问题
---
- P3
- 演示残留、命名混乱、重复代码和长期演进问题
{% /table %}

## 2. P0：最不能留在展示项目里的问题

### P0-01：密钥和默认凭据直接写进仓库

**位置：**

- `leego-parent/leego-web/src/main/resources/application.yml:13-15`
- `leego-parent/leego-web/src/main/resources/application.yml:25-42`
- `docker-compose.yml:10、37、68`
- `DEPLOYMENT.md:92-102、120`

**问题：**

- MySQL 密码默认为 `1234`。
- Redis 密码默认为 `123456`。
- JWT 密钥直接写在配置文件中。
- DashScope API Key 曾直接出现在 `application.yml` 和 `docker-compose.yml`。
- Compose 虽然注释说从环境变量读取，但后端环境中仍然存在硬编码 Key。

**为什么这是严重错误：**

这不是“配置写得不优雅”，而是凭据泄露。只要仓库被公开、截图被上传或 Git 历史被共享，第三方就可能调用模型、访问服务或伪造 Token。

**后果：**

- API Key 产生费用。
- 数据库和 Redis 被未授权访问。
- JWT 被伪造。
- 面试官看到后会直接怀疑整个项目的安全意识。

**正确做法：**

- 立即撤销并轮换已经暴露过的 DashScope Key。
- 所有秘密只从环境变量或本地未提交配置读取。
- 启动时缺少生产必需密钥应直接失败，不允许静默使用默认秘密。
- 提交 `.env.example`，只保留变量名和示例占位符。
- 清理 Git 历史中的敏感凭据。

### P0-02：短信自动注册却给用户设置固定默认密码

**位置：**

- `leego-parent/leego-server/src/main/java/com/leego/service/impl/UserServiceImpl.java:112-140`

**问题：**

业务设计是“未注册手机号通过验证码登录时自动注册”。这个业务本身没有问题，但实现给所有新用户设置了固定密码 `123456`。

**为什么错：**

验证码登录和密码登录是两种不同的身份凭据。自动注册用户一旦被设置固定密码，任何知道手机号的人都可能尝试用默认密码登录。

**正确业务规则：**

- 短信登录：手机号不存在则自动创建用户并登录。
- 自动创建的用户初始没有可用密码。
- 只有用户主动设置密码后，才允许密码登录。
- 密码登录只允许已有密码的账号使用。
- 数据库的 `password_hash` 应允许“尚未设置密码”的状态，或者增加 `password_set` 字段。

### P0-03：密码使用无盐 MD5

**位置：**

- `UserServiceImpl.java:181-185`
- `UserServiceImpl.java:447-455`

**问题：**

使用 `DigestUtils.md5DigestAsHex` 保存密码。

**为什么错：**

MD5 不是密码哈希算法，速度太快，也没有盐。数据库泄露后，弱密码极易被彩虹表和字典攻击破解。

**正确做法：**

- 使用 BCrypt、SCrypt 或 Argon2。
- 校验使用密码编码器的 `matches` 方法。
- 不要自行拼接“加盐 MD5”冒充安全方案。

### P0-04：短信验证码明文返回和日志记录

**位置：**

- `UserServiceImpl.java:143-161`
- `SmsCodeUtil.java:55-67`
- `leego-client/src/components/LeeGoDocExampleComponent.tsx:102-115`
- `leego-client/src/lib/api-client.ts:184-191`

**问题：**

- 后端接口直接把验证码放入响应。
- 服务端日志记录手机号和验证码。
- 前端弹窗展示验证码。
- 前端 Mock 固定返回 `123456`。

**为什么错：**

这会让“短信验证”退化成“调用接口拿验证码”。它只能作为明确隔离的演示模式，不能混在默认开发链路和正式代码中。

**正确做法：**

- 生产模式接入 `SmsSender`，响应只返回发送成功。
- 演示模式单独使用 `demo` profile，并在页面和文档中明确标注。
- 日志只记录脱敏手机号、请求 ID 和发送结果，不记录验证码。
- 验证码校验应限制错误次数，并在成功后原子删除。
- 使用 `SecureRandom`，不要使用普通 `Random`。

### P0-05：JWT Token 被完整写入日志

**位置：**

- `leego-parent/leego-common/src/main/java/com/leego/interceptor/JwtTokenInterceptor.java:26-37`

**问题：**

`log.info("jwt校验:{}", token)` 会把完整 Token 写入日志。

**后果：**

日志系统、容器日志、截图或错误收集平台一旦泄露，Token 在有效期内可以直接冒用。

**正确做法：**

- 永远不记录完整 Token。
- 只记录 `traceId`、用户 ID 和校验结果。
- Token 解析失败也不要把 Token 本身放入异常日志。

### P0-06：用户身份由请求体中的 `userId` 决定

**位置：**

- `AIShopSearchController.java:41-57`
- `AIShopSearchController.java:76-93`
- `AIShopSearchServiceImpl.java:152-186`
- `ProductAnalysisServiceImpl.java:70-71`

**问题：**

搜索和分析请求要求客户端传 `userId`，服务端直接使用它保存历史或记录日志。

**为什么错：**

客户端可以随意改 `userId`。这会导致：

- 把搜索历史写入别人的账号。
- 伪造分析请求的用户身份。
- 形成越权访问和审计污染。

**正确做法：**

- 从 JWT 解析当前用户 ID。
- Controller 从 `HttpServletRequest` 或统一的 `CurrentUser` 对象获取身份。
- 请求 DTO 不再接受可信的 `userId` 字段。
- 匿名搜索和登录后历史记录要明确区分。

### P0-07：前端内置测试 Token 可以绕过登录

**位置：**

- `leego-client/src/contexts/AuthContext.tsx:158-175`

**问题：**

只要密码等于 `test_token_123456`，前端就直接写入本地 Token 和用户信息，不经过后端。

**为什么错：**

这相当于在应用中留了一扇后门。即便它只是测试代码，也不应该存在于默认业务路径。

**正确做法：**

- 删除。
- 测试登录放到测试夹具、E2E 登录 helper 或独立测试环境。
- 不要在生产构建中保留测试分支。

## 3. P0：演示代码污染正式业务链路

### P0-08：开发环境默认开启 Mock，可能根本没调用后端

**位置：**

- `leego-client/src/lib/api-client.ts:11-21、53-56`

**问题：**

只要是开发环境且没有设置 `NEXT_PUBLIC_USE_REAL_API`，就直接使用 Mock。

**后果：**

- 页面看起来“功能正常”，但后端可能完全没有被调用。
- 前端开发者误以为接口契约已经正确。
- Apifox 测试通过，不代表真实前端链路通过。
- 新人接手项目时很难知道自己看到的是假数据还是真数据。

**正确做法：**

- 默认使用真实 API。
- Mock 必须显式开启，例如 `NEXT_PUBLIC_API_MODE=mock`。
- 页面上不要静默切换 Mock。
- Mock 数据放到测试夹具或 MSW 环境，不放在通用 API Client 的正式分支里。

### P0-09：Mock 分析结果使用随机数

**位置：**

- `leego-client/src/lib/api-client.ts:236-250、369-384`

**问题：**

理购值、推荐等级、分析文本通过 `Math.random()` 生成。

**为什么错：**

同一个商品每次刷新都可能得到不同结论。随机结果无法测试、无法解释、无法复现，也会把“AI 分析”伪装成真实能力。

**正确做法：**

- 演示数据必须固定。
- 测试结果必须可重复。
- 明确标记“Mock”。
- 真实链路失败时，只能返回可解释的降级结果，不能随机编造结论。

### P0-10：分类、收藏、历史等页面仍然有硬编码假数据

**位置：**

- `leego-client/src/lib/api-client.ts:263-358`
- `leego-client/src/app/category/page.tsx`
- `leego-client/src/app/api-test/page.tsx`

**问题：**

前端 API 层直接生成收藏列表、浏览历史、用户资料和商品数据。

**后果：**

页面可以展示一个“完整系统”，但用户实际操作不会落库，刷新后数据关系也不可信。

**正确做法：**

- 正式页面只依赖后端接口。
- 演示页面单独命名为 Demo。
- 不能让 Mock API 和真实 API 共用同一套默认方法。

### P0-11：存在没有实现的 `/register` 页面入口

**位置：**

- `leego-client/src/components/layout/Header.tsx:238-243`
- `leego-client/src/app` 目录中没有对应的 `register/page.tsx`

**问题：**

页面提供“注册”入口，但项目业务设计并没有独立注册流程，路由也不存在。

**正确做法：**

- 删除注册入口。
- 或将其改为“手机号验证码登录”，并明确说明未注册手机号会自动创建账号。
- 不要为了视觉上像完整产品而添加不存在的页面。

## 4. P1：数据建模和事实源错误

### P1-01：商品同时存在于 `goods.json`、MySQL 和 Redis，三份数据没有统一关系

**位置：**

- `leego-parent/leego-AI/src/main/resources/RAG/goods.json`
- `leego-parent/docker.mysql.init/init.sql:23-67`
- `leego-parent/leego-AI/src/main/java/com/leego/config/InitVectorDatabaseConfig.java:28-56`

**问题：**

- `goods.json` 被直接读入向量库。
- MySQL 定义了商品表，但初始化脚本没有导入这 75 个商品。
- Redis 向量文档又是一份派生数据。
- 三者的商品 ID、图片、平台报价和字段结构没有完成贯通。

**错误本质：**

没有定义“谁是事实源”。这会导致收藏、搜索、详情、分析看到的不是同一个商品。

**正确原则：**

- MySQL 是商品和平台报价的事实源。
- `goods.json` 只作为一次性种子数据。
- Redis Vector Store 只保存派生向量、索引版本和 `productId`。
- 任意时刻都可以从 MySQL 删除并重建向量索引。

### P1-02：数据库表设计了，但没有真实种子数据闭环

**位置：**

- `leego-parent/docker.mysql.init/init.sql`
- `leego-parent/leego-AI/src/main/resources/RAG/goods.json`

**问题：**

初始化 SQL 只创建表，没有把演示商品、平台和报价导入 MySQL。

**后果：**

- 数据库查询可能为空。
- 向量库却有商品。
- 搜索结果无法用 `productId` 回查事实。
- 部署后是否“有数据”取决于启动顺序和旧 Redis 数据。

**正确做法：**

- 编写幂等 seed/import 脚本。
- 先导入 MySQL，再同步向量。
- 为商品、平台、报价增加版本和更新时间。
- 新机器从零启动必须得到一致结果。

### P1-03：收藏表保存商品快照，却没有可靠事实关联

**位置：**

- `init.sql:98-111`
- `UserFavorite` 实体和相关 Service

**问题：**

收藏表同时保存 `product_id`、商品名、图片、价格和平台 JSON 快照，但商品外键没有建立成明确的事实关联，快照更新时间也没有定义。

**为什么不成熟：**

快照可以用于历史展示，但不能同时被当成当前商品事实。否则商品更新后，收藏页、详情页和分析页会互相矛盾。

**正确做法：**

- 当前收藏只保存 `userId + productId`。
- 展示时回查商品事实。
- 如果确实需要历史快照，单独命名为 snapshot，并保存生成时间和来源版本。

### P1-04：搜索历史字段叫 `result_product_ids`，实际保存的是商品名称

**位置：**

- `AIShopSearchServiceImpl.java:165-181`
- `init.sql:88-96`

**问题：**

代码提取的是 `product.name()`，再写入名为 `result_product_ids` 的 JSON 字段。

**后果：**

- 字段名和内容不一致。
- 商品改名后历史无法关联。
- 后续无法稳定跳转商品详情。

**正确做法：**

保存真实的 `productId` 数组，例如 `[1, 4, 9]`，展示时再回查商品名称。

### P1-05：商品字段和演示数据不具备支撑分析的事实密度

**位置：**

- `goods.json`
- `Product.java`
- `product-analysis-prompt.txt:13-18、31-57`

**问题：**

当前商品数据主要有名称、品牌、分类、空图片和平台价格，描述、规格、销量、评价、来源、更新时间等关键字段缺失。

但 Prompt 要求模型判断：

- 耐用性和寿命
- 性能参数
- 销量和市场占有率
- 品牌技术实力
- 保值率
- 质量水平

**错误本质：**

输入没有这些事实，Prompt 却要求模型给结论。这不是 RAG，而是让模型补写商品信息。

**正确做法：**

- 没有数据就明确返回“暂无依据”。
- 评分只使用已存在字段。
- 为每个事实保存来源、采集时间和可信度。
- 不把“未知”转换成“差”或“好”。

## 5. P1：RAG 设计中的典型错误

### P1-06：整个 `goods.json` 被 Token 切块，不是一商品一文档

**位置：**

- `InitVectorDatabaseConfig.java:34-40`

**问题：**

使用 `TextReader` 读取整个 JSON，再使用 `TokenTextSplitter` 切块。

**后果：**

- 一个向量块可能包含多个商品。
- 一个商品可能被切成半截。
- 召回后无法稳定获取 `productId`。
- 大模型收到的上下文可能是截断 JSON。

**正确做法：**

- 先解析 JSON 或从 MySQL 查询。
- 每个商品聚合成一个 `Document`。
- `Document.metadata` 至少包含 `productId`、品牌、分类、索引版本。
- 只对过长的商品详情做局部切块，而不是把商品集合整体切块。

### P1-07：初始化幂等标记在写入向量前设置

**位置：**

- `InitVectorDatabaseConfig.java:41-51`

**问题：**

先 `setIfAbsent(redisKey, "1")`，再执行 `vectorStore.add(list)`。

如果 Embedding 或 Redis 写入失败，初始化标志可能已经存在。下一次启动会跳过索引，系统进入“标记显示成功、实际上没有数据”的状态。

**正确做法：**

- 使用 `RUNNING/SUCCEEDED/FAILED` 状态。
- 只有全部写入成功后才写 `SUCCEEDED`。
- 索引同步必须可重试。
- 通过数据版本或 hash 判断是否需要重建，而不是只看一个永久标记。

### P1-08：查询文本把预算和性别拼进语义检索

**位置：**

- `AIShopSearchServiceImpl.java:117-127、197-206`

**问题：**

`keyword + 预算 + 性别` 被直接拼成 Embedding 查询文本。

**为什么错：**

预算和性别应当分别作为结构化过滤或业务排序条件。把它们混入语义向量，会污染“用户想买什么”的语义表示。

**正确做法：**

- Embedding 查询只使用规范化后的商品需求文本。
- 预算作为价格过滤和排序特征。
- 性别只有在商品数据确实有可靠适用人群字段时才能参与业务规则，不能默认影响召回。

### P1-09：无结果时重复调用相似度搜索，可能重复做 Embedding

**位置：**

- `AIShopSearchServiceImpl.java:203-231`

**问题：**

先用 `topK=3、threshold=0.7` 查询；无结果后再用同一个 query 调用 `topK=10、threshold=0.5`。

**后果：**

- 可能再次调用查询 Embedding。
- 延迟增加。
- 两套阈值没有离线评测依据。
- 日志写的是 `>=0.8`，实际代码是 `0.7`，参数和观测不一致。

**正确做法：**

- 一次生成查询向量，复用向量执行不同过滤或召回策略。
- 阈值由评测集确定。
- 统一配置、日志和指标。
- 记录每一阶段的耗时。

### P1-10：没有关键词召回、混合召回、RRF 和可解释重排

**位置：**

- `AIShopSearchServiceImpl.java:197-241`
- `README.md` 中的当前链路说明

**问题：**

当前核心路径基本是单一向量召回，然后交给大模型生成结果。

**后果：**

- 精确型号、品牌和数字参数容易被语义相似但型号错误的商品干扰。
- 无法解释为什么商品排在第一位。
- 没有 Recall@3、MRR、NDCG 等指标，无法证明 RAG 变好了。

**正确做法：**

目标链路应是：

```text
规范化查询
-> 关键词召回
-> 向量召回
-> RRF 融合
-> 预算、分类、品牌、数据完整度等业务重排
-> MySQL 回查事实
```

Cross-Encoder 或专用 Reranker 不应一开始就盲目加入，先建立评测集和可解释基线。

### P1-11：搜索阶段调用大模型重写商品 JSON

**位置：**

- `AIShopSearchServiceImpl.java:83-90、247-284`

**问题：**

向量召回之后再次调用 `qwenChatClient`，让模型生成商品列表和标签。

**为什么这是错误的 AI 使用方式：**

商品名称、图片、价格、平台报价都是结构化数据，应该由数据库和程序直接返回。大模型重写这些字段不能增加事实准确性，反而会带来：

- 2 到 5 秒级额外延迟。
- JSON 格式失败。
- 商品字段被模型改写。
- 结果不稳定。
- 搜索结果被模型幻觉污染。

**正确做法：**

搜索冷路径不调用生成式模型。大模型只放在需要自然语言解释的分析路径。

### P1-12：把 Embedding 误认为 Cross-Encoder

**涉及位置：**

- `leego-parent/leego-AI` 的 Embedding 配置
- `AIShopSearchServiceImpl.java:197-241`

**实际情况：**

`text-embedding-v4` 是分别编码查询和文档，再进行向量相似度检索，属于 Bi-Encoder/Dual-Encoder 召回。

它不是 Cross-Encoder，也不是专用 Reranker。

**反面教材：**

不要因为项目用了“向量数据库”和“Embedding”，就把系统描述成“Cross-Encoder 重排序”或“高级 RAG”。

**面试时应诚实表述：**

当前版本实现了 Embedding + Vector Store 召回；后续计划增加关键词混合召回和可解释业务重排，是否引入 Cross-Encoder 由评测结果决定。

## 6. P1：AI 分析和 Prompt 的越界

### P1-13：Prompt 要求模型执行程序应该执行的评分公式

**位置：**

- `product-analysis-prompt.txt:20-67`
- `ProductAnalysisServiceImpl.java:73-85`

**问题：**

Prompt 写了复杂的预算、市场表现、保值率和质量加权公式，却把计算交给模型。

**为什么错：**

模型不适合承担需要稳定、可测试、可复现的数值计算。相同输入可能因为模型版本、温度和上下文变化而得到不同分数。

**正确做法：**

- 程序计算 `ligou_value`。
- 程序计算分项分数、预算惩罚、数据完整度扣分。
- 模型只根据这些结果生成解释。
- 输出中带 `scoringVersion`，便于复现和回溯。

### P1-14：Prompt 强制生成无法证明的“法律警告”

**位置：**

- `product-analysis-prompt.txt:76-87、115-121`

**问题：**

模型可能依据极少的商品信息，自动生成《产品质量法》《广告法》等法律条款。

**后果：**

- 把“商品信息不足”误判成违法。
- 生成错误法条或错误适用范围。
- 将消费建议伪装成法律结论。

**正确做法：**

- 法律知识必须来自版本化、可追溯的法律知识库。
- 只有检索到明确证据后，模型才可以解释。
- 没有证据时返回“未发现可验证的相关信息”，不能自行编造条款。
- 页面上明确这不是法律意见。

### P1-15：Prompt 强制生成“文化底蕴”和“零成本替代方案”

**位置：**

- `product-analysis-prompt.txt:89-96、128-137`

**问题：**

这些字段不是每个商品都需要，也没有对应数据源。为了填满页面而要求模型输出，会制造大量无依据内容。

**正确做法：**

- 字段改为可选。
- 只有存在证据时才输出。
- 没有证据返回 `null` 或“不适用”。
- 不为了视觉效果向模型索要故事。

### P1-16：分析接口接受客户端传入的商品列表和价格

**位置：**

- `ProductAnalysisServiceImpl.java:73-85、135-159`
- `AIShopSearchController.java:76-93`

**问题：**

客户端把商品名称、平台、价格组装后发给后端，后端直接拿这些数据喂给模型。

**后果：**

- 用户可以修改价格，影响理购值。
- 用户可以注入任意商品文本。
- 分析结果不一定对应数据库中的商品。
- 搜索结果和分析结果可能互相矛盾。

**正确做法：**

分析请求只接受：

```json
{
  "productId": 75,
  "queryId": "q_xxx",
  "budget": 500
}
```

服务端根据 `productId` 和 `queryId` 从 MySQL 查询事实、从检索结果获取证据。

### P1-17：模型输出只靠去 Markdown 围栏后反序列化

**位置：**

- `ProductAnalysisServiceImpl.java:192-220`

**问题：**

代码只处理了 ```json 围栏，然后直接反序列化。没有：

- JSON Schema 校验。
- 字段范围校验。
- 枚举值校验。
- productId 对齐校验。
- evidenceId 校验。
- 模型输出版本校验。

**后果：**

模型只要返回一个错误字段、缺字段或多余解释，整个请求就失败。

**正确做法：**

- 使用结构化输出或 JSON Schema。
- 反序列化后进行 Bean Validation。
- 对分数、等级、证据 ID 做业务校验。
- 失败时返回程序计算的降级结果，而不是 500。

### P1-18：分析失败直接让整个请求失败，没有降级结果

**位置：**

- `ProductAnalysisServiceImpl.java:96-101`
- `AIShopSearchServiceImpl.java:281-284`
- `AIShopSearchController.java:63-66、99-102`

**问题：**

Embedding、模型、JSON 解析任意一步失败，最终都变成“AI 服务调用失败”。

**正确做法：**

至少提供以下降级层级：

1. 缓存中的已验证分析。
1. 程序计算的理购值和结构化证据。
1. 无自然语言解释但可用的商品事实。
1. 明确的错误状态和重试提示。

## 7. P1：性能和缓存设计错误

### P1-19：搜索冷路径做了不必要的两类 AI 调用

**位置：**

- `AIShopSearchServiceImpl.java:197-231、247-284`
- `ProductAnalysisServiceImpl.java:79-85`

**实际链路：**

```text
查询 Embedding
-> Redis 向量检索
-> 失败时可能再次检索/再次 Embedding
-> qwen3-max 重新生成搜索结果
-> 点击分析后再次调用生成模型
```

**问题判断：**

之前“两个致命调用”的说法有一半需要修正：

- 查询 Embedding 是语义检索的必要成本，不能简单说成“完全可以避免”。
- 真正可直接删除的，是搜索阶段的生成式模型调用。
- 无结果时重复调用向量查询是可以优化的额外成本。
- 点击分析时的模型调用是独立业务动作，不是搜索请求本身的重复。

### P1-20：缓存键没有绑定数据和 Prompt 版本

**位置：**

- `AIShopSearchServiceImpl.java:106-126`
- `ProductAnalysisServiceImpl.java:224-247`

**问题：**

分析缓存主要基于商品名称、预算和性别；搜索缓存主要基于拼接后的文本。没有绑定：

- 商品数据版本。
- 价格更新时间。
- 评分规则版本。
- Prompt 版本。
- 模型版本。
- 知识库版本。

**后果：**

商品价格已经变化，缓存仍可能返回旧结果；Prompt 改了，旧分析仍继续使用。

**正确做法：**

缓存键至少包含：

```text
业务类型
+ productId/queryId
+ normalizedQuery
+ budget
+ dataVersion
+ scoringVersion
+ promptVersion
+ model
```

### P1-21：分析和聊天共用模糊的 ChatClient 配置

**位置：**

- `leego-parent/leego-AI/src/main/java/com/leego/config/SaaLLMConfig.java`
- `leego-parent/leego-web/src/main/resources/application.yml:25-33`
- `ProductAnalysisServiceImpl.java:34-48`

**问题：**

配置文件写的是 `qwen-plus`，但自定义 Bean 又硬编码 `qwen3-max`；字段名叫 `qwen3ChatClient`，构造器参数叫 `qwenChatClient`。搜索、分析和聊天使用什么模型不够清晰。

**正确做法：**

明确拆分：

- `embeddingModel`
- `searchAnalysisModel`
- `supportChatModel`

模型名称通过配置注入，Bean 名称和字段名称保持一致。

## 8. P2：鉴权、会话和接口契约不成熟

### P2-01：退出登录实际上是空操作

**位置：**

- `UserServiceImpl.java:284-287`

**问题：**

`logout` 只打印日志，没有撤销 Refresh Token，也没有服务端黑名单或 Token 版本机制。

**说明：**

如果项目采用纯无状态 JWT，可以明确说明退出登录只清理客户端状态，并接受旧 Access Token 在过期前仍有效；但当前项目同时提供 Refresh Token，就应该至少撤销或轮换 Refresh Token。

### P2-02：Refresh Token 没有轮换和重放检测

**位置：**

- `UserServiceImpl.java:235-280`

**问题：**

Refresh Token 解析后直接签发新的 Access Token，没有记录使用状态、轮换链或重放检测。

**正确做法：**

- Refresh Token 使用随机 ID。
- 服务端保存哈希后的会话记录。
- 刷新时轮换 Refresh Token。
- 旧 Token 重放时撤销整条会话链。

### P2-03：前端把 Token 放在 localStorage

**位置：**

- `leego-client/src/contexts/AuthContext.tsx:31-36、140-145`
- `leego-client/src/lib/api-client.ts:24-28`

**问题：**

`localStorage` 方便，但 XSS 一旦发生，Token 可以被脚本读取。

**正确做法：**

生产场景优先考虑 HttpOnly、Secure、SameSite Cookie；如果暂时保留 localStorage，至少要完成 CSP、输入输出转义、依赖安全检查和明确的安全取舍说明。

### P2-04：短信接口只有手机号级限流

**位置：**

- `SmsCodeUtil.java:43-63`

**问题：**

当前只按手机号限制 60 秒发送一次，没有 IP、设备、失败次数和全局发送额度控制。

**后果：**

- 容易被换号、代理或脚本绕过。
- 可能产生短信费用。
- 验证码接口成为枚举和骚扰入口。

### P2-05：请求 DTO 混入了不该由客户端控制的字段

**涉及位置：**

- `AISearchRequest`
- `ProductAnalyzeRequest`
- 相关 Controller

**问题：**

客户端控制 `userId`、商品列表、价格、性别等字段，后端没有明确区分“用户偏好”和“服务端事实”。

**正确做法：**

DTO 应区分：

- `SearchCommand`：用户需求、预算、可选偏好。
- `ProductReference`：服务端生成的 `productId`。
- `AnalysisCommand`：`productId + queryId + budget`。
- `CurrentUser`：从 Token 获取，禁止客户端提交。

## 9. P2：工程实现和可维护性问题

### P2-06：依赖注入风格混用

**位置：**

- `AIShopSearchServiceImpl.java:45-46`
- `AIShopSearchController.java:28-32`
- 多个 Service 和 Controller

**问题：**

构造器注入、字段注入、`@Autowired` 混在一起。

**后果：**

- 单元测试需要 Spring 容器或反射。
- 依赖关系不清晰。
- 类在构造阶段无法保证依赖完整。

**正确做法：**

统一使用构造器注入，必要时使用 `@RequiredArgsConstructor`。

### P2-07：Controller 捕获所有异常并改写成模糊错误

**位置：**

- `AIShopSearchController.java:63-66、99-102`

**问题：**

Controller `catch (Exception)` 后统一返回“AI 服务调用失败”。

**后果：**

- 参数错误、权限错误、依赖超时、模型格式错误全部混为一谈。
- 前端无法决定是否重试。
- 运维无法根据响应判断根因。

**正确做法：**

- 业务异常交给全局异常处理器。
- 使用统一错误码。
- 对超时、限流、无数据、模型输出非法分别定义状态。
- 响应中返回 `traceId`。

### P2-08：异常日志可能记录过多业务输入

**位置：**

- `ProductAnalysisServiceImpl.java:217-220`
- 多处 `log.info(... request/DTO ...)`

**问题：**

模型原始响应和完整 DTO 可能进入日志。未来如果商品描述、用户输入或隐私数据增加，日志会成为数据泄露点。

**正确做法：**

- 只记录长度、哈希、traceId 和错误类型。
- 原始模型输出只在受控 debug 环境短期采样保存。
- 生产日志做脱敏和大小限制。

### P2-09：初始化逻辑放在 `@PostConstruct`，启动和外部依赖强耦合

**位置：**

- `InitVectorDatabaseConfig.java:32-56`

**问题：**

应用启动时直接读取文件、调用 Embedding、写 Redis。模型服务短暂不可用时，可能影响应用启动；数据量增加后，启动时间也会不可控。

**正确做法：**

- 将索引同步做成独立命令或管理任务。
- 应用启动只检查索引状态。
- 必须自动同步时，也要异步执行、可重试、可观测。

### P2-10：`System.out` 与日志框架混用

**位置：**

- `InitVectorDatabaseConfig.java:47、52、55`

**问题：**

初始化代码直接使用 `System.out.println`，无法统一日志级别、traceId、结构化字段和采集策略。

**正确做法：**

统一使用 SLF4J，并避免在日志中输出密钥、验证码、Token 和完整 Prompt。

### P2-11：前端存在重复的示例组件和 API 封装

**位置：**

- `leego-client/src/components/LeeGoExampleComponent.tsx`
- `leego-client/src/components/LeeGoDocExampleComponent.tsx`
- `leego-client/src/lib/api.ts`
- `leego-client/src/services/ApiService.ts`
- `leego-client/src/lib/api-client.ts`

**问题：**

项目同时保留了示例组件、文档组件、多个 API 封装和测试页面。新旧接口风格并存，容易造成“改了一个调用，另一个仍然旧逻辑”的问题。

**正确做法：**

- 正式业务只保留一套 API Client。
- 示例代码移到 `examples/` 或删除。
- 测试页面明确命名为开发工具，不挂在正式导航。

### P2-12：容器内和浏览器侧都把后端写成 localhost

**位置：**

- `leego-client/next.config.ts:9-17`
- `leego-client/src/app/api/proxy/route.ts:4`
- `leego-client/src/app/api/chat/route.ts:4`
- `leego-client/src/app/api/fetch-url/route.ts:4`
- `docker-compose.yml:95-99`

**问题：**

容器内的 `localhost` 指向前端容器自己；浏览器中的 `localhost` 指向访问者自己的电脑。两种网络语义混在一起。

**后果：**

- 本地开发能跑，Compose 中失败。
- 远程部署后浏览器请求打到用户电脑。

**正确做法：**

- 服务间请求使用 Compose 服务名，例如 `http://backend:8080`。
- 浏览器访问使用同源反向代理或公开域名。
- 通过环境变量区分 server-side URL 和 browser-side URL。

### P2-13：健康检查配置与应用能力没有闭环

**位置：**

- `docker-compose.yml:82-87`
- `docker-compose.yml:107-112`

**问题：**

Compose 检查 `/actuator/health`，但项目是否启用了 Actuator、是否暴露该端点、是否区分 readiness，没有形成已验证闭环。

**正确做法：**

- 明确添加 Actuator 依赖和暴露策略。
- 使用 `/actuator/health/readiness`。
- 为 MySQL、Redis、DashScope 区分必需依赖和可降级能力。
- 在实际 Docker 启动后验证，而不是只做 YAML 解析。

### P2-14：依赖使用 `latest`

**位置：**

- `docker-compose.yml:33`

**问题：**

Redis 使用 `redis/redis-stack:latest`，构建结果会随时间变化。

**后果：**

- 今天能跑，之后可能因镜像升级失败。
- 无法复现面试演示环境。

**正确做法：**

固定镜像版本，并记录验证日期。

## 10. P2：测试缺失不是“小问题”

### P2-15：只有人工 Apifox 测试，没有可重复回归

**现状：**

项目主要依靠 Apifox 手工验证，自动化测试不足。

**为什么不够：**

人工测试只能证明某次操作成功，不能证明：

- 改完搜索逻辑后收藏仍然正常。
- Token 过期后刷新链路正确。
- 模型返回坏 JSON 时系统能降级。
- 数据库和 Redis 重建后结果一致。
- 前端真实环境没有走 Mock。

**至少需要补充：**

{% table %}
- 层级
- 测试内容
---
- 单元测试
- 查询规范化、RRF、业务重排、理购值、缓存键
---
- Controller 契约测试
- 参数校验、错误码、鉴权、响应字段
---
- 集成测试
- MySQL、Redis、商品导入、向量索引
---
- 模型适配测试
- 超时、限流、坏 JSON、空响应
---
- 前端测试
- 搜索状态、分析状态、SSE 错误、降级展示
---
- E2E
- 登录、搜索、收藏、分析、历史、反馈
---
- 检索评测
- Recall@3、MRR、NDCG、无关结果率
{% /table %}

### P2-16：没有固定评测集，无法证明 RAG 改进有效

**问题：**

没有一组固定查询和人工标注的相关商品，因此任何“召回变好了”的结论都只是感觉。

**正确做法：**

先建立 30 至 50 条查询，覆盖：

- 精确型号。
- 品牌和类别。
- 同义词。
- 预算内和预算外。
- 拼写错误。
- 无结果查询。
- 长自然语言需求。

记录 `query`、`budget`、相关 `productId` 和不可出现的商品，定期计算 Recall@3、MRR 和 NDCG。

## 11. P3：项目叙事和文档上的错误

### P3-01：把未实现功能写成项目能力

当前不能写成已实现能力的内容包括：

- Kafka。
- 自动爬虫。
- 实时多平台比价。
- 价格预警。
- 评论分析。
- 维权文书生成。
- 图片和视频宣传分析。

这些可以作为“规划方向”，不能写进现状介绍、简历完成项或演示脚本。

### P3-02：把离线模拟价格说成真实比价

**真实情况：**

商品和平台价格来自离线演示数据，不是实时爬虫采集，也没有统一采集时间、商品链接和数据来源。

**正确说法：**

“基于离线多平台报价样例实现商品检索和价格快照展示。”

不要说：

“实时抓取淘宝、京东、拼多多并完成全网最低价比较。”

### P3-03：截图里的评分和风险提示不能当成事实能力

**位置：**

- `docs/images/analysis-demo.png`
- `README.md` 中的演示说明

截图可以证明页面做出来了，但不能证明评分经过数据集验证，也不能证明法律提示准确。

展示时要明确：

- 这是演示链路输出。
- 当前评分规则和证据集仍在完善。
- 不构成商品质量鉴定或法律意见。

### P3-04：README 曾经混淆“当前实现”和“目标方案”

例如同时描述当前使用 qwen3-max 搜索生成，又把混合召回、RRF、证据 RAG 写成目标能力。新人和面试官会很难判断哪些是真实代码，哪些是规划。

**正确做法：**

README 固定分为：

1. 当前已实现。
1. 当前已知限制。
1. 正在整改。
1. 远期路线。

### P3-05：代码注释、日志和命名暴露出复制粘贴式迭代

典型表现：

- `qwen3ChatClient` 字段实际注入 `qwenChatClient`。
- 日志中的相似度阈值和代码不一致。
- `result_product_ids` 实际存商品名。
- 类和注释中混有“演示”“测试”“AI 推荐”“搜索”等不同阶段的称呼。
- 部分代码被整段注释保留，形成“死代码仓库”。

**正确做法：**

- 删除失效实现，不要长期注释保存。
- 命名表达真实职责。
- 注释解释“为什么”，不要重复“做了什么”。
- 日志输出和代码参数从同一配置读取。

## 12. 这些问题为什么会集中出现

### 12.1 先追求页面效果，再补业务事实

页面先需要“评分、标签、风险、推荐”，于是模型被要求生成这些内容；但后端没有足够商品事实，模型只能猜。

### 12.2 先把所有组件接上，再定义边界

Redis、向量库、Embedding、ChatClient、SSE、缓存和 Mock 都接入了，但没有先定义：

- 商品事实在哪里。
- 哪些字段由程序决定。
- 哪些内容允许模型生成。
- 哪些输出必须有证据。

### 12.3 先让它能演示，再忘记拆掉演示支架

固定验证码、默认密码、前端 Mock、测试 Token、随机评分、硬编码价格和 localhost 都是比赛赶工时合理的临时手段，但它们没有在进入工程版前被隔离。

### 12.4 用手工成功替代系统性验证

“Apifox 能调通”只能证明某个接口在某个输入下返回了结果，不能证明整个系统的身份、数据、缓存、模型和前端状态是一致的。

## 13. 整改优先级

### 第一阶段：P0，先让项目可信

1. 轮换所有暴露过的 Key。
1. 删除硬编码密码和 JWT Secret。
1. 删除测试 Token 和默认 Mock 模式。
1. 修复短信自动注册的密码状态。
1. 改用 BCrypt/Argon2。
1. 删除验证码和 JWT 完整日志。
1. 所有用户身份从 Token 获取。
1. 修复前后端容器地址。

### 第二阶段：P1，重建商品和 RAG 主链路

1. `goods.json` 只作为 seed。
1. 先导入 MySQL。
1. 统一 `productId`。
1. 一商品一 Document。
1. 关键词召回 + 向量召回 + RRF。
1. 业务规则重排。
1. 删除搜索阶段生成式模型调用。
1. 服务端根据 `productId` 回查商品事实。
1. 建立检索评测集。

### 第三阶段：P2，补齐 AI 分析工程能力

1. 程序计算理购值。
1. 建立证据 ID 和来源。
1. 分析模型只解释证据。
1. 结构化输出和 Schema 校验。
1. SSE 使用类型化事件。
1. 模型超时、坏 JSON 和限流时降级。
1. 分析结果按版本持久化。
1. 搜索、分析、聊天拆分客户端和记忆空间。

### 第四阶段：P2/P3，补齐可维护性

1. 单元测试。
1. Controller 契约测试。
1. Testcontainers 集成测试。
1. 前端组件测试和 Playwright E2E。
1. 指标、Trace、阶段耗时。
1. 清理示例组件、重复 API Client 和死代码。
1. 更新 README 和演示脚本。

## 14. 面试时如何诚实讲这个项目

可以这样说：

> 这是一个比赛期间完成的可运行功能演示版，早期实现采用了离线商品 JSON、Redis Vector Store 和 DashScope Embedding，并在搜索后用生成模型组织结果。复盘后发现，搜索阶段让大模型重写结构化商品数据既增加延迟，也引入事实幻觉；同时商品事实源、向量文档和用户身份没有完全贯通。后续整改计划是以 MySQL 作为商品事实源，Redis 只保存派生向量，通过关键词和向量混合召回、RRF 和可解释业务重排完成搜索；理购值由程序计算，模型只基于证据生成解释，并通过 Schema、缓存、降级和检索评测保证可控性。

不要说：

- “使用了 Cross-Encoder”，因为当前并没有。
- “实现了实时全网比价”，因为当前是离线模拟数据。
- “AI 自动识别商品质量和违法行为”，因为当前证据不足。
- “实现了完整 RAG 平台”，因为当前仍处于整改阶段。

## 15. 最终判断

这个项目最值得保留的不是“模型很大”，而是它已经暴露出了很多真实 AI 工程会遇到的问题：

- 数据事实和向量索引如何分层。
- 什么时候该用 Embedding，什么时候需要 Reranker。
- 哪些任务应该交给程序，哪些任务才适合交给大模型。
- 如何处理模型不稳定、输出不可信和证据不足。
- 如何让缓存、身份、数据版本和分析结果能够追踪。

完成 P0 到 P3 的核心整改后，它可以成为一个不错的实习项目；但在整改前，最准确的定位仍然是：

> 有真实代码和完整界面的 AI 商品消费决策功能演示版，工程化和 RAG 可信度仍需补齐。
